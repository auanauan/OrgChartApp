# ระบบสร้างผังองค์กร (Org Chart Application)

โปรเจคนี้พัฒนาด้วย [Angular CLI](https://github.com/angular/angular-cli) เวอร์ชัน 21.0.5 เป็นระบบสร้างและจัดการผังองค์กรแบบ Interactive ที่ใช้งานง่ายและมีฟีเจอร์ครบครัน

## 🌟 ฟีเจอร์หลัก

### 1. การจัดการตำแหน่ง (Position Management)
- เพิ่มตำแหน่งใหม่พร้อมรหัสตำแหน่ง
- แสดงรายการตำแหน่งทั้งหมดในแผงด้านข้าง
- Drag & Drop ตำแหน่งไปยัง level ต่างๆ

### 2. การสร้างผังองค์กร
- สร้าง Level ได้ไม่จำกัด
- ลาก Position จากแผงด้านข้างไปวาง
- ระบบจะถามให้เลือก Parent Node (ยกเว้น Level 1)
- แสดงเส้นเชื่อมระหว่าง Parent-Child แบบ Orthogonal
- แต่ละเส้นเชื่อมมีสีต่างกัน และสีเดียวกับขอบล่างของ Node

### 3. การแสดงผล
- Node แบบการ์ดสีขาวพร้อมขอบสีตามเส้นเชื่อม
- แสดงชื่อตำแหน่งและรหัสตำแหน่ง
- Hover เพื่อดู Parent และจำนวน Children
- แสดง Badge บอก Parent และ Children เมื่อ Hover
- เส้นเชื่อมจะ Highlight เมื่อ Hover ที่ Node

### 4. การลบและจัดการ
- ลบ Node พร้อมเลือกว่าจะลบลูกทิ้งหรือย้ายลูกขึ้นไปต่อ Parent
- ลบ Level ทั้ง Level (ยกเว้น Level 1)
- ระบบจะจัดลำดับ Level ใหม่อัตโนมัติเมื่อลบ

### 5. การบันทึกข้อมูล
- บันทึกข้อมูลลง localStorage
- โหลดข้อมูลอัตโนมัติเมื่อเปิดหน้าใหม่
- กดปุ่ม "Save all" เพื่อบันทึกทุกครั้งที่แก้ไข

## 🚀 การติดตั้งและรันโปรเจค

### ติดตั้ง Dependencies
```bash
npm install
```

### รัน Development Server
```bash
ng serve
```

เปิดเบราว์เซอร์ไปที่ `http://localhost:4200/` แอปพลิเคชันจะ reload อัตโนมัติเมื่อแก้ไขโค้ด

### Build สำหรับ Production
```bash
ng build
```

ไฟล์ build จะอยู่ในโฟลเดอร์ `dist/`

## 📖 วิธีการใช้งาน

### 1. เพิ่มตำแหน่ง (Add Position)
1. คลิกปุ่ม **+** ในแผง "Position" ด้านขวา
2. กรอกชื่อตำแหน่งและรหัสตำแหน่ง
3. คลิก Confirm เพื่อเพิ่มตำแหน่งใหม่

### 2. สร้างผังองค์กร
1. **Level 1**: ลาก Position จากแผงไปวางที่ Level 1 (เป็น Root Node)
2. **Level อื่นๆ**: 
   - คลิกปุ่ม **+** ด้านล่างเพื่อเพิ่ม Level ใหม่
   - ลาก Position ไปวางที่ Level ที่ต้องการ
   - เลือก Parent Node จาก Dialog ที่แสดงขึ้นมา
   - คลิก Confirm

### 3. ดูรายละเอียด Node
- **Hover** ที่ Node เพื่อดู:
  - Badge แสดง Parent และจำนวน Children ด้านบน
  - ข้อมูล Parent และ Children ใน Node
  - เส้นเชื่อมจะเปลี่ยนเป็นสีน้ำเงิน
  - Parent Node และ Child Nodes จะมี Highlight พิเศษ

### 4. ลบ Node
1. Hover ที่ Node ที่ต้องการลบ
2. คลิกปุ่ม **×** สีแดงมุมขวาบน
3. เลือกว่าจะ:
   - **ลบโหนดนี้และลูกทั้งหมด**: ลบทิ้งทั้งหมด
   - **ลบโหนดนี้และย้ายลูกขึ้นไปต่อกับโหนดแม่**: ย้ายลูกไปต่อ Parent
4. คลิก Yes เพื่อยืนยัน

### 5. ลบ Level
1. Hover ที่ Label ของ Level (Level 2, 3, 4...)
2. คลิกปุ่ม **×** สีแดงที่ปรากฏขึ้น
3. ยืนยันการลบ (จะลบ Node ทั้งหมดใน Level นั้นด้วย)
4. ระบบจะจัดลำดับ Level ใหม่อัตโนมัติ

### 6. บันทึกข้อมูล
1. คลิกปุ่ม **Save all** ด้านล่างกลาง
2. ข้อมูลจะถูกบันทึกลง localStorage
3. เมื่อ Refresh หน้า ข้อมูลจะโหลดกลับมาอัตโนมัติ

## 🎨 เทคโนโลยีที่ใช้

- **Angular 21.0.5**: Framework หลัก
- **Angular Material**: UI Components (Dialog, Button, Icon, Snackbar)
- **Angular CDK Drag & Drop**: สำหรับลากวาง
- **TypeScript**: ภาษาที่ใช้พัฒนา
- **RxJS**: จัดการ State แบบ Observable
- **SVG**: วาดเส้นเชื่อมระหว่าง Node
- **LocalStorage**: บันทึกข้อมูล

## 📁 โครงสร้างโปรเจค

```
src/
├── app/
│   ├── components/
│   │   ├── org-chart/          # Component หลักสำหรับแสดงผัง
│   │   ├── position-dialog/    # Dialog เพิ่มตำแหน่ง
│   │   ├── confirm-dialog/     # Dialog ยืนยันการลบ
│   │   └── parent-selector-dialog/  # Dialog เลือก Parent
│   ├── models/
│   │   └── org-chart.models.ts # Type definitions
│   ├── services/
│   │   └── org-chart.service.ts # Service จัดการข้อมูล
│   └── app.ts                  # App Component
```

## 🔧 คำสั่งเพิ่มเติม

### Generate Component ใหม่
```bash
ng generate component component-name
```

### รัน Unit Tests
```bash
ng test
```

### ดู Help เพิ่มเติม
```bash
ng generate --help
```

## 💡 Tips การใช้งาน

1. **เริ่มต้นใช้งาน**: เพิ่มตำแหน่งใน Position panel ก่อน แล้วค่อยสร้างผัง
2. **สีเส้นเชื่อม**: แต่ละ connection จะมีสีต่างกัน ขอบล่าง Node จะมีสีตรงกับเส้นที่เชื่อมมา
3. **Drag & Drop**: สามารถลาก Node ไปวางใหม่ได้ (จะถามเลือก Parent ใหม่)
4. **บันทึกบ่อยๆ**: กด Save all เพื่อไม่ให้ข้อมูลหายเมื่อ refresh
5. **ลบ Level**: ใช้เมื่อต้องการปรับโครงสร้างใหม่ทั้ง Level

## 👨‍💻 Developer

Developed by Thanakrit Mangkang
